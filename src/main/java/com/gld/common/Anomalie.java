package com.gld.common;

import com.gld.bean.DemandeBean;

public class Anomalie{
	//verifier que le type de dossier copie dossier est un appel de IDCE
	public Boolean checkRGGLD001(String appClient,String typemessage){
		return null;
	}
	public Boolean checkFluxEntrantValide(DemandeBean demande){
		//if (demande.getCodeIRT()==null ||(demande.getCodeIRT().length()==20 || demande.getCodeIRT().length()==23) || (demande.getCodeIRT().length()==23))
		return null;
	}
	
}